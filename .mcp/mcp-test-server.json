{
  "mcpServers": {
    "teach-niche-test": {
      "command": "node",
      "args": ["scripts/mcp-test-server.js"],
      "env": {
        "DATABASE_URL": "postgresql://testuser:testpass@localhost:5433/teachniche_test",
        "REDIS_URL": "redis://localhost:6380",
        "NODE_ENV": "test"
      }
    }
  }
}